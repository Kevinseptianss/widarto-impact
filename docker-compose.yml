services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: widarto-impact-web
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    env_file:
      - .env.local
